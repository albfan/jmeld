application.args=
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/JMeld.jar
dist.javadoc.dir=${dist.dir}/javadoc
file.reference.forms-1.0.5.jar=../../usr/local/kees/project/projecten/jmeld/lib/forms-1.0.5.jar
file.reference.jmeld-images=../../usr/local/kees/project/projecten/jmeld/images
file.reference.jmeld-lib=lib
file.reference.jmeld-lib-1=../../usr/local/kees/project/projecten/jmeld/lib
file.reference.jmeld-src=../../usr/local/kees/project/projecten/jmeld/src
file.reference.looks-2.0.jar=../../usr/local/kees/project/projecten/jmeld/lib/looks-2.0.jar
file.reference.projecten-jmeld=.
jar.compress=false
javac.classpath=\
    ${file.reference.forms-1.0.5.jar}:\
    ${file.reference.looks-2.0.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
main.class=org.jmeld.JMeld
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=-Xmx400m -Xms100m
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
src.dir=${file.reference.jmeld-src}
